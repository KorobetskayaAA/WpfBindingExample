<Application x:Class="WpfBindingExample.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfBindingExample"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             StartupUri="Example5\Example5Window.xaml">
    <Application.Resources>
        <sys:Double x:Key="BaseFontSize">19</sys:Double>
        <ValueSource x:Key="BigFont"  />
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="{StaticResource BaseFontSize}" />
            <Setter Property="Margin" Value="7,2"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="{StaticResource BaseFontSize}" />
            <Setter Property="Margin" Value="7,0"/>
        </Style>
    </Application.Resources>
</Application>
